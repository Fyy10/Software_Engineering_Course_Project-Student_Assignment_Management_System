<!--pages/student/join_lesson.wxml-->
<view class="page__bd">
    <view class="weui-search-bar {{inputShowed ? 'weui-search-bar_focusing' : ''}}" id="searchBar">
        <form class="weui-search-bar__form">
            <view class="weui-search-bar__box">
                <i class="weui-icon-search"></i>
                <input type="text" class="weui-search-bar__input" placeholder="请输入想要搜索的课程名" value="{{inputVal}}"
                     focus="{{inputShowed}}" bindinput="inputTyping" />  
                <span class="weui-icon-clear" wx:if="{{inputVal.length > 0}}" bindtap="clearInput"></span>
            </view>
            <label class="weui-search-bar__label" bindtap="showInput">
                <i class="weui-icon-search"></i>
                <span class="weui-search-bar__text">搜索课程</span>
            </label>
        </form>
        <view class="weui-search-bar__cancel-btn" bindtap="hideInput">取消</view>
    </view>
</view>

<view wx:if="{{inputVal.length > 0}}" >
  <radio-group bindchange="radioChange">
      <label class="weui-cell weui-check__label" wx:for="{{match_list}}" wx:key="index">
        <view class="weui-cell weui-cell_active weui-cell_access" >
          <view class="weui-cell__bd">
            <text style="vertical-align: middle;">{{item.name}}</text>
            <text class="weui-badge" style="margin-left: 10px;">{{item.teacher}}</text>
          </view>
          <view class="cell__right">
              <radio value="{{item.teacher}}"></radio>
          </view>
        </view>
      </label>
  </radio-group>
</view>
<view wx:if="{{inputVal.length > 0}}">
    <a class="weui-btn weui-btn_primary" bindtap="Confirm">新建课程</a>
</view>

<!-- 成功提示 -->
<view class="fadeIn {{hideToast ? 'fadeOut' : ''}}" wx:if="{{toast}}">
    <view class="weui-mask_transparent"></view>
    <view class="weui-toast">
        <i class="weui-icon-success-no-circle weui-icon_toast"></i>
        <view class="weui-toast__content">加入课程成功</view>
    </view>
</view>
